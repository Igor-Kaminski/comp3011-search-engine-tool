COMP3011 Coursework 2 Video Script

Target time: 4:30 to 4:55.
Do not go over 5:00.

Main rule:
- Show the commands.
- Keep the code walkthrough very short.
- Only show suggest/stats if you have spare time.

============================================================
0:00 - 0:15 INTRO
============================================================

SAY:
This is my COMP3011 Coursework 2 search engine tool.

It crawls quotes.toscrape.com, builds an inverted index,
saves it to disk, and lets the user search from the command
line.

============================================================
0:15 - 2:20 LIVE DEMO
============================================================

RUN:
python -m src.main

SAY:
The required commands are build, load, print, and find.

RUN:
build

SAY WHILE BUILD RUNS:
Build regenerates data/index.json.

The pauses are intentional.
The crawler waits 6 seconds between website requests to
respect the politeness window.

When it finishes, it should show 10 crawled pages.

RUN:
load

SAY:
Load reads the saved index.

RUN:
print nonsense

SAY:
Print shows the inverted index for one word:
pages, frequency, and positions.

RUN:
find indifference

SAY:
This is single-word search.

RUN:
find good friends

SAY:
This is multi-word search.
It returns pages containing both words and ranks them
with TF-IDF.

RUN:
find "good friends"

SAY:
This is an extra phrase-search feature.
It uses word positions to check the exact phrase.

OPTIONAL IF TIME:
RUN:
suggest indiference

SAY:
This is an extra suggestion command for misspellings.

OPTIONAL IF TIME:
RUN:
stats

SAY:
This is an extra command showing index statistics.

RUN:
find

SAY:
Empty queries are handled with a usage message.

RUN:
find qwertywordnotfound

SAY:
Missing words return no results instead of crashing.

============================================================
2:20 - 3:05 SHORT CODE WALKTHROUGH
============================================================

SHOW:
src/crawler.py
Scroll to:
- crawl()
- _fetch()

SAY:
In crawler.py, crawl() follows pagination.

_fetch() sends requests and applies the 6-second delay.

SHOW:
src/indexer.py
Scroll to:
- tokenize()
- build_inverted_index()

SAY:
tokenize() lowercases words, so search is case-insensitive.

build_inverted_index() creates:

word -> page URL -> frequency and positions

SHOW:
src/search.py
Scroll to:
- find()
- _tf_idf_score()
- _contains_phrase()

SAY:
find() does the search by intersecting posting lists.

_tf_idf_score() ranks results.

_contains_phrase() uses positions for exact phrase search.

SHOW:
src/main.py
Scroll to:
- execute()

SAY:
main.py connects the shell commands to the implementation.

============================================================
3:05 - 3:35 TESTING
============================================================

RUN:
python -m pytest --cov=src --cov-report=term-missing

SAY:
The tests cover the crawler, indexer, search logic, CLI,
integration workflow, and performance.

Crawler tests use fake HTTP sessions, so tests do not hit
the live website or wait 6 seconds.

The result is 28 tests passing with 90 percent coverage.

============================================================
3:35 - 3:55 VERSION CONTROL
============================================================

RUN:
git log --oneline --decorate --max-count=8

SAY:
The Git history shows development in stages:
core implementation, CLI, tests, advanced search,
documentation, benchmarking, snippets, and final updates.

============================================================
3:55 - 4:45 GENAI REFLECTION
============================================================

SAY:
I used OpenAI Codex and I am declaring that use.

Codex helped interpret the brief, scaffold the project,
write code, improve tests, and draft documentation.

It helped most with project structure and test ideas.

However, I still had to verify the code myself.

For example, I checked that the saved index matched a fresh
crawl and repeatedly ran the test suite.

One problem was that Python cache files were included early,
so I fixed that with .gitignore and cleaned the repo.

This showed me that AI-generated work still needs review,
testing, and understanding.

Overall, the project meets build, load, print, and find.

It also adds TF-IDF ranking, phrase search, suggestions,
snippets, statistics, validation, automated tests,
and benchmarking.

Thank you.

============================================================
IF STILL TOO LONG
============================================================

Skip:
- suggest
- stats
- the phrase search command
- one or two Git sentences

Do not skip:
- build
- load
- print
- find
- tests
- GenAI declaration
